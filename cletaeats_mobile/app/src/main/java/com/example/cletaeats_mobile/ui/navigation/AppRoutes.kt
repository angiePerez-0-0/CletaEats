package com.example.cletaeats_mobile.ui.navigation

/**
 * Centralización de rutas de navegación.
 * Cambiar un nombre aquí lo cambia en todo el app.
 */
object AppRoutes {
    const val LOGIN        = "login"
    const val REGISTER     = "register"

    // ── Rutas CLIENTE ────────────────────────────────────────────
    const val RESTAURANTES = "restaurantes"

    // ── Rutas REPARTIDOR ─────────────────────────────────────────
    const val PEDIDOS_REPARTIDOR = "pedidos_repartidor"

    // ── Splash / selector de rol ─────────────────────────────────
    const val SPLASH = "splash"
}