package com.example.cletaeats.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Paleta CletaEats ────────────────────────────────────────────
// Naranja cálido (comida, energía, apetito) + fondo oscuro moderno
// Coherente con apps de delivery: UberEats usa negro/verde,
// Rappi usa naranja. CletaEats: naranja + gris oscuro + blanco.

val CletaNaranja        = Color(0xFFFF6B00)   // Principal — acción
val CletaNaranjaOscuro  = Color(0xFFCC5500)   // Pressed / variante
val CletaNaranjaClaro   = Color(0xFFFFAD70)   // Tonal — fondos suaves

val CletaGrisOscuro     = Color(0xFF1A1A2E)   // Fondo principal
val CletaGrisMedio      = Color(0xFF16213E)   // Surface / cards
val CletaGrisClaro      = Color(0xFF0F3460)   // Surface variant

val CletaBlanco         = Color(0xFFFFFFFF)
val CletaTextoSecundario = Color(0xFFB0BEC5)
val CletaError          = Color(0xFFCF6679)
val CletaExito          = Color(0xFF4CAF50)

// ─── Light theme (menos usado, se puede mejorar después) ─────────
val CletaNaranjaLight   = Color(0xFFFF6B00)
val CletaFondoLight     = Color(0xFFFAFAFA)
val CletaSurfaceLight   = Color(0xFFFFFFFF)